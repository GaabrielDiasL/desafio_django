version: '3'

services:
  web:
    build: .
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000 &&
             python manage.py cron_import_files"
    volumes:
      - .:/code
      - dbdata:/var/lib/sqlite
    ports:
      - "8000:8000"

volumes:
  dbdata:
